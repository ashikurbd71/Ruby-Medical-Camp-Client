import React from 'react';
import SectionTitle from '../../../Shared/SectionTitle';

const ManagesRegister = () => {
    return (
        <div>
            <SectionTitle heading={'Manages Register'}/>
        </div>
    );
};

export default ManagesRegister;